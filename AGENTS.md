- [ ] # AGENTS.md 正式版

  
  用途：本文件是用于约束所有 AI 编程工具以及人工开发者在本项目中的协作、规划、编码、测试、部署和文档维护行为。  
  路径规则：本文档中的路径示例均使用相对路径。
  
  ## 目录

  - [1. 核心原则](#1-核心原则)
  - [2. AI Vibe Coding 工作原则](#2-ai-vibe-coding-工作原则)
    - [2.1 人负责边界，AI 负责执行](#21-人负责边界ai-负责执行)
    - [2.2 先定义，再实现](#22-先定义再实现)
    - [2.3 小步快跑，每步闭环](#23-小步快跑每步闭环)
    - [2.4 简单优先](#24-简单优先)
    - [2.5 外科手术式修改](#25-外科手术式修改)
    - [2.6 快速通道机制](#26-快速通道机制)
  - [3. 角色分工](#3-角色分工)
    - [3.1 产品经理角色](#31-产品经理角色)
    - [3.2 架构师角色](#32-架构师角色)
    - [3.3 工程师角色](#33-工程师角色)
    - [3.4 测试 / 验收角色](#34-测试--验收角色)
  - [4. 上下文与任务边界](#4-上下文与任务边界)
    - [4.1 一个上下文只做一个阶段](#41-一个上下文只做一个阶段)
    - [4.2 上下文输入优先级](#42-上下文输入优先级)
    - [4.3 需要开启新上下文的情况](#43-需要开启新上下文的情况)
    - [4.4 不让 AI 猜关键事实](#44-不让-ai-猜关键事实)
    - [4.5 AI 输出验证机制](#45-ai-输出验证机制)
  - [5. 项目结构规范](#5-项目结构规范)
    - [5.1 根目录文件](#51-根目录文件)
    - [5.2 目录用途](#52-目录用途)
  - [6. 文档管理规范](#6-文档管理规范)
    - [6.1 文档通用规则](#61-文档通用规则)
      - [6.1.1 文档更新优先级策略](#611-文档更新优先级策略)
    - [6.2 必备文档](#62-必备文档)
    - [6.3 `readme.md` 必须包含的模块](#63-readmemd-必须包含的模块)
    - [6.4 `PRD.md` 必须包含的模块](#64-prdmd-必须包含的模块)
    - [6.5 `docs/ProcessTodo.md` 必须记录](#65-docsprocesstodomd-必须记录)
  - [7. PRD、里程碑与 Spec 规范](#7-prd里程碑与-spec-规范)
    - [7.1 基本关系](#71-基本关系)
    - [7.2 推荐流程](#72-推荐流程)
    - [7.3 Spec 文档命名](#73-spec-文档命名)
    - [7.4 Spec 文档模板](#74-spec-文档模板)
    - [7.5 里程碑文档模板](#75-里程碑文档模板)
  - [8. webMock 原型规范](#8-webmock-原型规范)
    - [8.1 定义](#81-定义)
    - [8.2 推荐目录结构](#82-推荐目录结构)
    - [8.3 生命周期](#83-生命周期)
  - [9. 开发实施流程](#9-开发实施流程)
    - [9.1 完整功能开发流程](#91-完整功能开发流程)
    - [9.2 Bug 修复流程](#92-bug-修复流程)
    - [9.3 重构流程](#93-重构流程)
  - [10. 代码修改规范](#10-代码修改规范)
    - [10.1 修改前](#101-修改前)
    - [10.2 修改中](#102-修改中)
    - [10.3 修改后](#103-修改后)
  - [11. 配置、端口与密钥规范](#11-配置端口与密钥规范)
    - [11.1 配置规范](#111-配置规范)
    - [11.2 端口规范](#112-端口规范)
    - [11.3 密钥规范](#113-密钥规范)
  - [12. Docker 与部署规范](#12-docker-与部署规范)
  - [13. 数据库规范](#13-数据库规范)
  - [14. 测试与验收规范](#14-测试与验收规范)
    - [14.1 测试优先级](#141-测试优先级)
    - [14.2 测试记录](#142-测试记录)
    - [14.3 验收标准](#143-验收标准)
  - [15. Git 版本管理规范](#15-git-版本管理规范)
  - [16. 安全与数据规范](#16-安全与数据规范)
    - [16.1 AI 输入安全](#161-ai-输入安全)
    - [16.2 文件与日志安全](#162-文件与日志安全)
  - [17. 开发环境配置](#17-开发环境配置)
    - [17.1 网络与代理](#171-网络与代理)
    - [17.2 开发工具配置](#172-开发工具配置)
    - [17.3 本地调试配置](#173-本地调试配置)
    - [17.4 环境变量管理](#174-环境变量管理)
  - [18. AI 编程工具通用使用规范](#18-ai-编程工具通用使用规范)
    - [18.1 适合交给 AI 编程工具的任务](#181-适合交给-ai-编程工具的任务)
    - [18.2 任务输入规则](#182-任务输入规则)
    - [18.3 执行规则](#183-执行规则)
    - [18.4 禁止事项](#184-禁止事项)
  - [19. 多会话协作规范](#19-多会话协作规范)
    - [19.1 需要开启新会话的情况](#191-需要开启新会话的情况)
    - [19.2 推荐会话类型](#192-推荐会话类型)
    - [19.3 会话交接规则](#193-会话交接规则)
    - [19.4 并行会话规则](#194-并行会话规则)
  - [20. 禁止事项与反模式](#20-禁止事项与反模式)
    - [20.1 需求反模式](#201-需求反模式)
    - [20.2 架构反模式](#202-架构反模式)
    - [20.3 编码反模式](#203-编码反模式)
    - [20.4 协作反模式](#204-协作反模式)
  - [21. 每次任务完成检查清单](#21-每次任务完成检查清单)
    - [21.1 通用必须检查项](#211-通用必须检查项)
    - [21.2 代码修改任务检查项](#212-代码修改任务检查项)
    - [21.3 文档变更任务检查项](#213-文档变更任务检查项)
    - [21.4 快速通道任务检查项](#214-快速通道任务检查项)
    - [21.5 验收标准](#215-验收标准)
  
  ## 1. 核心原则

  - 所有路径必须使用相对路径；禁止在项目文档、配置、脚本和代码中写入本机绝对路径。
  - 所有端口都不要使用常用端口，并且必须做成可配置项。
  - 项目必须使用 Docker 技术，至少需要能够在 Linux OS 中部署运行。
  - 所有可配置内容都不要在代码中写死，必须提供配置文件，并在需要用户维护时提供配置维护 Web 页面。
  - 项目至少必须包含 `README.md`、`PRD.md` 和 `docs/` 文档目录。
  - 所有文档必须有目录。
  - 每次功能、接口、配置、数据库、部署、页面交互或测试方式变更后，必须同步更新对应文档。
  - 正式代码禁止直接指向、引用、导入、链接或运行依赖 `webMock/` 中的内容。
  - 开发过程必须使用 Todo 方式管理，并在 `docs/ProcessTodo.md` 中记录已完成、待完成、阻塞项、需要用户配置和需要用户提供的内容。
  - 修改代码后必须运行必要测试或检查；如果无法运行，必须说明原因、影响和替代验证方式。
  - AI 和人工开发都必须遵守"目标明确、范围明确、可验证、文档同步"的交付闭环。
  - **LLM 应用选型规则**：涉及 LLM、Agent、RAG、工具调用、Prompt 管理、记忆或多模型路由时，优先使用已有依赖和成熟框架。
    1. 如果项目已经使用 LangChain、LlamaIndex、Semantic Kernel、Vercel AI SDK、OpenAI SDK、Anthropic SDK 等相关依赖，优先沿用已有依赖和封装方式。
    2. 如果项目已经使用 LangChain，优先复用 LangChain 的 model、prompt、tool、retriever、vector store、document loader、chain 或 runnable，不重复实现一套并行抽象。
    3. 如果需要 RAG、工具编排、复杂链路、可观测性或多模型适配，评估 LangChain、LlamaIndex 或项目现有 AI 框架。
    4. Prompt、模型名、温度、超时、重试、API base URL 和密钥必须通过配置或环境变量维护，不得写死生产配置。
  
  ## 2. AI Vibe Coding 工作原则

  AI vibe coding 的关键不是让 AI 一次性写完整系统，而是把 AI 当成高执行力工程团队使用。

  ### 2.1 人负责边界，AI 负责执行

  - 人定义业务目标、优先级、范围、不做范围和验收标准。
  - AI 负责提出方案、拆解任务、修改代码、补充测试、更新文档和总结风险。
  - AI 不应把未确认的聊天内容直接变成代码。
  - 未进入 `PRD.md`、`docs/Specs/`、`docs/ProcessTodo.md` 或测试用例的内容，不应被视为已确认需求。
  
  ### 2.2 先定义，再实现

  编码前必须明确：

  - 本次目标；
  - 成功标准；
  - 允许修改范围；
  - 明确不做范围；
  - 影响的页面、接口、数据、配置、部署或文档；
  - 验证方式。
  
  没有 PRD 或 Spec 的功能，不直接进入正式开发。小修复可以使用轻量任务卡，但仍必须写清楚成功标准。

  ### 2.3 小步快跑，每步闭环

  推荐任务粒度：

  - 一个 bug；
  - 一个接口；
  - 一个页面状态；
  - 一个配置项；
  - 一个数据库迁移；
  - 一组相关测试；
  - 一份明确文档。
  
  不推荐任务粒度：

  - "帮我做完整后台系统"；
  - "把这个项目优化一下"；
  - "你看着改"；
  - "把所有问题都修掉"。
  
  ### 2.4 简单优先

  - 不做用户未要求的功能。
  - 不做单次使用的过度抽象。
  - 不做无关重构。
  - 不为了"看起来专业"引入复杂架构。
  - 如果 50 行可以解决，不写 200 行。
  
  ### 2.5 外科手术式修改

  - 只修改当前任务必须修改的内容。
  - 不格式化无关文件。
  - 不重命名无关变量。
  - 不顺手改注释、目录结构或技术栈。
  - 发现无关问题时，只记录或反馈，不擅自修复。
  
  ### 2.6 快速通道机制

  为提升小修改的效率，以下情况可以使用简化流程：

  **适用场景：**

  - 修改少于 30 行代码的 bug 修复；
  - 纯文档格式调整或错别字修正；
  - 配置文件默认值调整；
  - 单文件内的简单重构（如变量重命名）。
  
  **快速通道要求：**

  - 在聊天中明确说明预期行为和验证方式；
  - 修改后简要说明验证结果；
  - 无需更新 `docs/ProcessTodo.md`，但需在提交信息中说明；
  - 如果修改涉及接口或配置变更，仍需更新相关文档。
  
  **不适用快速通道的情况：**

  - 新增功能或模块；
  - 涉及数据库变更；
  - 涉及 API 接口变更；
  - 需要新增依赖；
  - 影响多个文件的修改。
  
  ## 3. 角色分工

  | 阶段     | 主要任务                                | 主要产物                                    |
  | -------- | --------------------------------------- | ------------------------------------------- |
  | 产品构思 | 梳理目标、用户、范围、MVP、验收标准     | `PRD.md`、`docs/Milestones/`                |
  | 架构设计 | 技术选型、模块边界、风险分析、接口草案  | `docs/TechnicalValidation/`、`docs/Design/` |
  | 规格拆解 | 从 PRD 拆 Spec，定义任务卡和测试方案    | `docs/Specs/`                               |
  | 代码实现 | 阅读代码、修改文件、运行命令、修测试    | `app/`、`src/`、`tests/`                    |
  | 代码审查 | 检查范围漂移、bug、过度设计、文档遗漏   | Review 记录、Todo                           |
  | 验收交付 | 跑测试、检查 Docker、更新文档、总结风险 | `docs/TestReport.md`、`docs/ProcessTodo.md` |
  
  ### 3.1 产品经理角色

  职责：

  - 梳理业务目标、用户角色和使用场景；
  - 定义功能范围、优先级和 MVP；
  - 输出或维护 `PRD.md`；
  - 输出里程碑目标和验收标准。
  
  禁止：

  - 直接给出未经验证的技术实现细节；
  - 在开发过程中临时扩大范围但不更新 PRD 或 Spec。
  
  ### 3.2 架构师角色

  职责：

  - 拆分模块边界；
  - 定义数据流、配置流和部署边界；
  - 对关键技术方案做可行性验证；
  - 输出 Spec、架构图、风险分析和技术验证结论。
  
  禁止：

  - 为不存在的扩展点设计复杂架构；
  - 在没有验证的情况下把方案写成确定结论。
  
  ### 3.3 工程师角色

  职责：

  - 根据已确认 Spec 或任务卡实现功能；
  - 只修改当前任务必须修改的内容；
  - 保持配置可维护、端口可配置、Docker 可部署；
  - 编写或更新测试；
  - 修改完成后同步更新文档。
  
  禁止：

  - 未经确认擅自扩大功能范围；
  - 直接从聊天记录中实现未落入 Spec 的内容；
  - 把配置项、端口、路径、密钥写死在代码中。
  
  ### 3.4 测试 / 验收角色

  职责：

  - 根据 Spec 和 PRD 验收标准设计测试；
  - 验证正常流程、异常流程、边界条件、权限、安全和配置项；
  - 记录测试结果和遗留风险。
  
  主要产物：

  - `tests/`
  - `docs/TestReport.md`
  - Spec 中的测试方案和验收记录。
  
  ## 4. 上下文与任务边界

  ### 4.1 一个上下文只做一个阶段

  推荐按阶段拆分上下文：

  1. 产品需求上下文；
  2. 架构设计上下文；
  3. Spec 拆解上下文；
  4. 单个开发任务上下文；
  5. Review / 验收上下文。
  
  当用户从"讨论需求"跳到"直接写代码"时，AI 应先检查是否已有 Spec；没有 Spec 时先补 Spec。

  ### 4.2 上下文输入优先级

  AI 执行任务时，事实来源优先级如下：

  1. `PRD.md`
  2. `docs/Milestones/`
  3. `docs/Specs/`
  4. `docs/ProcessTodo.md`
  5. 代码和测试文件
  6. 当前聊天补充说明
  
  如果聊天补充改变了事实，必须同步写入对应文档。

  ### 4.3 需要开启新上下文的情况

  - 从产品规划切换到代码实现；
  - 从 A 模块切换到 B 模块；
  - 从探索方案切换到执行落地；
  - 上下文中已有大量废弃方案；
  - AI 开始反复引用过期结论；
  - 需求、设计或验收标准发生重大变化。
  
  ### 4.4 不让 AI 猜关键事实

  AI 必须遵守：

  - 不确定就说不确定；
  - 有多种理解就列出差异；
  - 关键业务规则缺失时先问；
  - 不能把假设悄悄写进代码；
  - 不能把未确认建议当成已确认需求。
  
  ### 4.5 AI 输出验证机制

  AI 在提供技术方案或建议时，必须明确说明以下验证信息：

  **必须声明：**

  - [ ] **方案验证状态**：该方案是否已在当前项目环境中验证过
  - [ ] **已知限制**：是否存在已知的使用限制或边界条件
  - [ ] **依赖确认**：所依赖的库、工具或版本是否已确认可用
  - [ ] **信息时效性**：所引用的技术信息是否存在过时风险
  
  **需要人工确认的情况：**

  - 涉及关键业务规则或安全策略的决策
  - 可能影响系统架构或数据流的设计方案
  - 需要权衡多个技术选型的场景
  - 涉及第三方服务集成或外部依赖的方案
  
  **发现信息可能过时的处理：**

  - 明确告知用户该信息的可能时效性问题
  - 建议用户通过官方文档或最新资源进行验证
  - 提供验证该信息的具体方法或命令
  
  ## 5. 项目结构规范

  项目根目录建议采用以下结构。不同技术栈可以在此基础上微调，但新增、删除或调整目录时，必须同步更新文档。

  ```text
  .
  ├── readme.md
  ├── AGENTS.md
  ├── PRD.md
  ├── docker-compose.yml
  ├── Dockerfile
  ├── .env.example
  ├── docs/
  │   ├── Index.md
  │   ├── ProcessTodo.md
  │   ├── DatabaseSchema.md
  │   ├── Deployment.md
  │   ├── Config.md
  │   ├── TestReport.md
  │   ├── Milestones/
  │   ├── Specs/
  │   ├── ProductIterations/
  │   ├── TechnicalValidation/
  │   ├── Design/
  │   └── References/
  ├── webMock/
  ├── app/
  ├── src/
  ├── config/
  ├── scripts/
  ├── tests/
  ├── deploy/
  └── data/
  ```
  
  ### 5.1 根目录文件

  | 路径                 | 用途                                                         |
  | -------------------- | ------------------------------------------------------------ |
  | `readme.md`          | 项目入口说明，必须包含功能介绍、程序部署方式、运行环境、操作方式、常用命令。 |
  | `AGENTS.md`          | AI/开发协作规则，定义项目内执行任务时必须遵守的规范。        |
  | `PRD.md`             | 产品需求文档，描述业务目标、用户角色、功能范围、页面交互、验收标准等。 |
  | `docker-compose.yml` | 本地和服务器部署编排，端口、环境变量、数据卷必须可配置。     |
  | `Dockerfile`         | 应用镜像构建文件，必须支持 Linux OS 中构建和运行。           |
  | `.env.example`       | 环境变量示例文件，记录所有可配置项，不存放真实密钥。         |
  
  ### 5.2 目录用途

  | 路径       | 用途                                                         |
  | ---------- | ------------------------------------------------------------ |
  | `docs/`    | 所有项目文档统一存放目录。                                   |
  | `webMock/` | 正式开发前的前端 Mock 原型目录，仅供参考，正式代码不得直接依赖。 |
  | `app/`     | 应用入口或按框架约定存放页面、路由、服务入口等内容。         |
  | `src/`     | 通用源码目录，存放业务逻辑、组件、服务、工具函数等。         |
  | `config/`  | 配置文件、配置加载逻辑和配置默认值。                         |
  | `scripts/` | 项目脚本，如初始化、构建、部署辅助、数据迁移等。             |
  | `tests/`   | 自动化测试，包括单元测试、接口测试、集成测试等。             |
  | `deploy/`  | 部署相关文件，如反向代理模板、Linux 服务脚本、部署说明片段等。 |
  | `data/`    | 本地开发数据、样例数据或挂载目录。真实生产数据不得提交到 Git。 |
  
  ## 6. 文档管理规范

  ### 6.1 文档通用规则

  - 所有项目文档必须有目录。
  - 所有路径示例必须使用相对路径。
  - 文档中不得出现真实密钥、真实 Token、真实密码、生产隐私数据。
  - 文档必须说明用途、适用范围和维护规则。
  - 文档内容必须和代码、配置、数据库、部署方式保持一致。
  - 每次功能、接口、配置、数据库、部署、页面交互或测试方式变更后，必须同步更新对应文档。
  - 如果内容暂时无法确认，必须标记"待确认"，并写清楚需要谁提供什么信息。
  - 如果内容废弃，不能悄悄删除，必须先标记"已废弃"并说明替代文档或废弃原因。
  - 同一个事实只能有一个主文档作为权威来源，其他文档引用或摘要即可。
  - 文档更新完成后，必须在 `docs/ProcessTodo.md` 中记录更新内容和影响范围。
  
  ### 6.1.1 文档更新优先级策略

  为平衡文档质量与开发效率，文档更新按优先级处理：

  **P0 - 必须立即更新：**

  - `PRD.md`（如果需求发生变更）
  - `docs/Specs/` 下的相关 Spec（如果实现与 Spec 不符或 Spec 本身需要调整）
  - `docs/ProcessTodo.md`（记录本次修改的完成状态）
  - `docs/TestReport.md`（如果测试方案或结果发生变化）
  
  **P1 - 建议同步更新：**

  - `readme.md`（如果功能介绍、部署方式或常用命令发生变化）
  - `docs/Config.md`（如果配置项发生变化）
  - `docs/Deployment.md`（如果部署方式发生变化）
  
  **P2 - 可以批量更新：**

  - 其他参考文档
  - 示例代码
  - 架构图或设计文档
  
  **批量更新建议：** 每完成 3-5 个相关任务后，统一进行一次 P2 级别文档的整理和更新。

  建议每份文档顶部包含：

  ```text
  # 文档标题
  
  > 状态：draft / reviewing / confirmed / deprecated
  > 最后更新：YYYY-MM-DD
  > 适用范围：说明本文档覆盖什么，不覆盖什么
  
  ## 目录
  ```
  
  ### 6.2 必备文档

  项目至少必须包含：

  - `readme.md`
  - `PRD.md`
  - `docs/Index.md`
  - `docs/ProcessTodo.md`
  - 如果有数据库：`docs/DatabaseSchema.md`
  - 如果有配置项：`docs/Config.md`
  - 如果支持部署：`docs/Deployment.md`
  - 如果进入正式开发：对应的 `docs/Specs/` 文档
  
  ### 6.3 `readme.md` 必须包含的模块

  - 功能介绍；
  - 程序部署方式；
  - 运行环境；
  - 操作方式；
  - 常用命令。
  
  ### 6.4 `PRD.md` 必须包含的模块

  - 项目概述；
  - 目标与范围；
  - 用户与角色；
  - 业务流程；
  - 功能需求；
  - 页面与交互；
  - 数据模型；
  - 接口定义；
  - 业务规则；
  - 非功能要求；
  - 验收标准；
  - 技术与实现约束。
  
  ### 6.5 `docs/ProcessTodo.md` 必须记录

  - 已实现的功能；
  - 待实现的功能；
  - 当前阻塞问题；
  - 需要用户配置的内容；
  - 需要用户提供的内容；
  - 每次变更影响到的文档；
  - 每次测试验证结果；
  - 本次修改对应的 PRD、Spec、里程碑或 Mock。
  
  ## 7. PRD、里程碑与 Spec 规范

  ### 7.1 基本关系

  - PRD 面向"为什么做、为谁做、做什么"。
  - 里程碑面向"当前阶段做什么、不做什么、如何验收"。
  - Spec 面向"如何按规格实现、如何测试、如何验收"。
  - 不能从 PRD 直接跳到代码，必须经过 MVP、里程碑和 Spec 拆解。
  - Spec 是 AI 或研发执行开发的输入契约，必须足够明确，避免靠聊天上下文临时补充。
  
  ### 7.2 推荐流程

  ```text
  PRD.md
    ↓
  确定产品里程碑
    ↓
  确定最小 MVP 版本
    ↓
  拆分模块和用户故事
    ↓
  输出 Spec 文档
    ↓
  基于 Spec 开发
    ↓
  测试与验收
    ↓
  更新文档与 Todo
  ```
  
  ### 7.3 Spec 文档命名

  ```text
  docs/Specs/SPEC-001-模块名称.md
  docs/Specs/SPEC-002-页面名称.md
  docs/Specs/SPEC-003-接口名称.md
  ```
  
  ### 7.4 Spec 文档模板

  ```text
  # SPEC-编号-名称
  
  ## 目录
  
  ## 1. 背景与目标
  ## 2. 关联 PRD 条目
  ## 3. 适用范围
  ## 4. 不做范围
  ## 5. 用户故事
  ## 6. 页面与交互
  ## 7. 接口定义
  ## 8. 数据模型
  ## 9. 配置项
  ## 10. 业务规则
  ## 11. 异常与边界情况
  ## 12. 权限与安全
  ## 13. 测试方案
  ## 14. 验收标准
  ## 15. 文档更新清单
  ## 16. 待确认问题
  ```
  
  ### 7.5 里程碑文档模板

  ```text
  # MILESTONE-编号-名称
  
  ## 目录
  
  ## 1. 阶段目标
  ## 2. MVP 范围
  ## 3. 本阶段必须交付
  ## 4. 本阶段明确不做
  ## 5. 关联 Spec
  ## 6. 验收标准
  ## 7. 风险与依赖
  ## 8. 需要用户提供
  ## 9. 需要用户配置
  ## 10. 完成记录
  ```
  
  ## 8. webMock 原型规范

  ### 8.1 定义

  `webMock/` 是正式开发前的前端原型与交互验证目录，用于沉淀页面草图、静态 HTML、交互 Demo、样例数据、截图、原型说明和评审记录。它的定位是"设计输入"和"开发参考"，不是正式运行时代码的一部分。

  必须遵守：

  - 项目开始编写正式前端代码前，前端 Mock 原型相关文件必须放在 `webMock/`。
  - `webMock/` 中的内容可以被正式代码参考、复制、改写或重构。
  - 正式代码禁止直接指向、引用、导入、链接或运行依赖 `webMock/` 中的文件。
  - `webMock/` 中不得存放真实密钥、真实用户数据、生产接口地址或生产数据库连接信息。
  - 当前端原型转为正式实现后，必须在对应 Spec 和 `docs/ProcessTodo.md` 中记录来源、取舍、变更点和验收结果。
  
  ### 8.2 推荐目录结构

  ```text
  webMock/
  ├── README.md
  ├── pages/
  ├── components/
  ├── assets/
  ├── data/
  ├── flows/
  ├── screenshots/
  └── review/
  ```
  
  ### 8.3 生命周期

  ```text
  需求输入
    ↓
  原型创建
    ↓
  原型评审
    ↓
  PRD / Spec 映射
    ↓
  正式开发参考
    ↓
  实现验收
    ↓
  归档或迭代
  ```
  
  生命周期规则：

  - `webMock/README.md` 必须维护每个页面或流程的当前状态。
  - 状态从 `accepted` 进入 `implementing` 前，必须存在对应 Spec。
  - 状态到 `implemented` 后，必须在 `docs/ProcessTodo.md` 中记录"原型已落地"或"与原型存在差异"。
  - 如果正式实现与 Mock 不一致，必须以 PRD / Spec / 用户确认结果为准，并记录差异原因。
  
  ## 9. 开发实施流程

  ### 9.1 完整功能开发流程
  
  ```text
  需求输入
    ↓
  PRD 更新
    ↓
  里程碑 / MVP 确认
    ↓
  Mock / Design（如涉及页面）
    ↓
  Spec 拆解
    ↓
  任务 Todo
    ↓
  代码实现
    ↓
  自动化测试 / 手工验证
    ↓
  Docker / 部署验证（如涉及部署）
    ↓
  文档同步
    ↓
  Review / 验收
  ```
  
  ### 9.2 Bug 修复流程
  
  ```text
  复现问题
    ↓
  写明预期行为与实际行为
    ↓
  补测试或最小复现步骤
    ↓
  修复代码
    ↓
  运行相关测试
    ↓
  更新 TestReport / ProcessTodo
    ↓
  反馈原因、修复点和验证结果
  ```
  
  ### 9.3 重构流程
  
  ```text
  说明重构目的
    ↓
  列出不改变的外部行为
    ↓
  先跑现有测试
    ↓
  小步重构
    ↓
  再次跑测试
    ↓
  说明行为未变和风险
  ```
  
  未经用户要求，不主动进行大规模重构。
  
  ## 10. 代码修改规范

  ### 10.1 修改前

  必须先完成：

  - 读取相关 PRD / Spec / Todo；
  - 明确成功标准；
  - 列出简短计划；
  - 确认影响范围；
  - 判断是否需要测试先行；
  - 如果有多种理解，先列出差异并确认。
  
  ### 10.2 修改中

  必须遵守：

  - 匹配项目现有风格；
  - 优先复用已有工具函数和模式；
  - 只加必要代码；
  - 不把配置写死；
  - 不把本机路径写死；
  - 不把真实密钥、Token、账号提交；
  - 不直接依赖 `webMock/`；
  - 不改无关文件。
  
  ### 10.3 修改后

  必须完成：

  - 清理因本次修改产生的未使用代码；
  - 运行最小必要测试；
  - 更新相关文档；
  - 总结修改文件；
  - 说明验证结果；
  - 说明遗留风险和待确认事项。
  
  ## 11. 配置、端口与密钥规范
  
  ### 11.1 配置规范
  
  - 所有可配置项必须有配置文件或环境变量入口。
  - 所有配置项必须记录到 `.env.example` 和 `docs/Config.md`。
  - 需要用户维护的配置必须提供配置维护 Web 页面，至少支持查看、修改、保存和恢复默认值。
  - 默认值可以存在，但不能把业务环境、生产地址、密钥写死到代码。
  
  ### 11.2 端口规范
  
  - 不使用常用端口作为默认端口。
  - 端口必须可配置。
  - Docker 映射端口必须通过环境变量或配置文件维护。
  - 端口变化必须同步更新 `readme.md`、`docs/Deployment.md`、`.env.example` 和 `docs/Config.md`。
  
  ### 11.3 密钥规范

  禁止提交：

  - 真实密码；
  - 真实 Token；
  - 真实密钥；
  - 生产数据库连接；
  - 真实用户隐私数据；
  - 内部不可公开地址。
  
  ## 12. Docker 与部署规范
  
  项目至少提供：
  
  - `Dockerfile`
  - `docker-compose.yml`
  - `.env.example`
  - `docs/Deployment.md`
  
  必须保证：
  
  - 至少能在 Linux OS 中部署运行；
  - 容器端口、宿主机端口、数据卷、外部服务地址可配置；
  - 镜像中不包含本机绝对路径；
  - 部署命令可复制执行；
  - 常用运维命令写入 `readme.md` 或 `docs/Deployment.md`。
  
  ## 13. 数据库规范
  
  如果项目包含数据库，必须创建并维护 `docs/DatabaseSchema.md`。
  
  每张表必须说明：

  - 表名；
  - 表用途；
  - 字段名；
  - 字段类型；
  - 字段含义；
  - 是否必填；
  - 默认值；
  - 索引；
  - 关联关系；
  - 影响范围。
  
  每次增加、删除或修改表和字段时，必须同步更新 `docs/DatabaseSchema.md`，并在 `docs/ProcessTodo.md` 中记录变更原因和验证结果。
  
  如果当前无数据库，也要在 `docs/DatabaseSchema.md` 中写明"当前无数据库"。
  
  ## 14. 测试与验收规范

  ### 14.1 测试优先级

  从高到低：

  1. 能复现 bug 的自动化测试；
  2. 当前模块单元测试；
  3. 接口或集成测试；
  4. 构建检查；
  5. Docker 启动验证；
  6. 手工验证步骤。
  
  ### 14.2 测试记录
  
  `docs/TestReport.md` 必须记录：
  
  - 测试时间；
  - 测试范围；
  - 测试命令；
  - 测试数据；
  - 通过或失败结果；
  - 未覆盖内容；
  - 遗留风险。
  
  不能只写"已测试"，必须写清楚怎么测和结果。
  
  ### 14.3 验收标准
  
  任务完成必须满足：
  
  - 功能符合 Spec；
  - 不做范围未被实现；
  - 相关测试通过，或已说明无法运行的原因和替代验证；
  - 文档已同步；
  - 配置和部署说明一致；
  - 无硬编码密钥、端口和本机路径；
  - 用户待确认事项已明确列出。
  
  ## 15. Git 版本管理规范
  
  - 每次功能修改应保持提交粒度清晰，一个提交只解决一个主要问题。
  - 提交前必须确认：
    - 相关测试已运行；
    - 文档已同步更新；
    - 没有提交真实密钥、临时文件、大体积无关文件；
    - 没有包含与当前任务无关的格式化或重构。
  
  建议提交信息格式：
  
  ```text
  类型: 简短说明
  ```
  
  常用类型：
  
  - `feat`：新增功能；
  - `fix`：修复问题；
  - `docs`：文档变更；
  - `test`：测试变更；
  - `chore`：构建、脚本、配置等非业务变更。
  
  ## 16. 安全与数据规范
  
  ### 16.1 AI 输入安全
  
  向 AI 提供上下文时：
  
  - 不贴真实密钥；
  - 不贴生产用户数据；
  - 不贴未脱敏日志；
  - 不贴包含隐私的数据库导出；
  - 必要时先脱敏或构造样例数据。
  
  ### 16.2 文件与日志安全

  - `.env`、生产配置、密钥文件不得提交到 Git。
  - 样例配置使用 `.env.example`。
  - 日志样例必须脱敏。
  - `webMock/` 和 `data/` 中不得存放真实用户数据。
  
  ## 17. 开发环境配置
  
  ### 17.1 网络与代理
  
  遇到依赖安装、资料拉取、镜像构建等网络问题时，可以尝试以下方案：
  
  **镜像源切换：**
  
  - npm/yarn/pnpm：配置国内镜像源（如淘宝镜像、腾讯云镜像）
  - Docker：配置镜像加速器
  - Python pip：配置清华、阿里云等镜像源
  - Maven/Gradle：配置阿里云镜像
  
  **代理配置：**
  
  - 如需使用代理，在 `.env` 或配置文件中设置，不要硬编码在代码中
  - 代理配置应支持 HTTP/HTTPS/SOCKS5 协议
  - 提供代理配置的验证命令
  
  ### 17.2 开发工具配置
  
  **推荐配置：**
  
  - IDE/编辑器：统一代码格式化规则（如 `.editorconfig`、Prettier、ESLint）
  - Git：配置提交模板和钩子（pre-commit、commit-msg）
  - 终端：统一使用项目指定的包管理器和脚本命令
  
  **环境一致性：**
  
  - 使用 Docker 确保开发环境一致性
  - 提供 `docker-compose.dev.yml` 用于本地开发
  - 文档中说明各开发环境的配置步骤
  
  ### 17.3 本地调试配置
  
  - 提供本地调试的配置示例（如 `.vscode/launch.json`、IDE 运行配置）
  - 说明如何连接本地开发数据库或测试服务
  - 提供 Mock 数据或测试账号（脱敏）
  
  ### 17.4 环境变量管理
  
  - 所有环境相关的配置必须通过环境变量或配置文件管理
  - 提供 `.env.example` 作为配置模板
  - 区分开发、测试、生产环境的配置差异
  - 敏感配置（密钥、密码）不得提交到版本控制
  
  ## 18. AI 编程工具通用使用规范
  
  本规范面向所有 AI 编程工具，不区分具体产品名称。不同工具能力可以不同，但在本项目中的输入、执行、验证和交付规则必须一致。
  
  ### 18.1 适合交给 AI 编程工具的任务
  
  - 阅读项目结构和相关文档；
  - 根据 PRD、里程碑或 Spec 拆解任务；
  - 小范围代码修改；
  - 修复可复现 bug；
  - 补充或调整测试；
  - 调整配置、脚本和 Docker 文件；
  - 更新项目文档；
  - 运行构建、测试、lint 或其他检查命令；
  - 生成变更摘要、风险清单和待确认问题。
  
  ### 18.2 任务输入规则
  
  交给 AI 编程工具的任务必须尽量包含：
  
  - 当前角色；
  - 本次目标；
  - 参考文档和文件；
  - 允许修改范围；
  - 明确不做范围；
  - 配置、端口、Docker、数据库、文档等约束；
  - 验收标准；
  - 验证方式；
  - 最终反馈格式。
  
  不允许只给出"你看着改""整体优化""全部重构"这类无边界指令。

  ### 18.3 执行规则

  - 先读相关文件，再提出简短计划。
  - 修改前说明假设、成功标准和影响范围。
  - 优先使用项目已有脚本、测试、目录结构和代码风格。
  - 不引入新依赖，除非 Spec 明确要求或已经说明必要性和影响范围。
  - 修改时只触碰当前任务必须修改的文件。
  - 修改后运行最小必要验证。
  - 如果测试或命令无法运行，必须说明原因、影响和替代验证方式。
  - 完成后必须说明修改内容、验证结果、文档更新和遗留风险。
  
  ### 18.4 禁止事项

  - 禁止在没有 PRD、Spec、任务卡或明确验收标准的情况下大范围改项目。
  - 禁止在同一个任务里同时扩大需求、重构架构、修改无关模块和补充新功能。
  - 禁止为了通过测试删除测试或降低验收标准。
  - 禁止把配置、端口、路径、密钥和生产地址写死。
  - 禁止提交无法解释影响范围的改动。
  - 禁止只输出代码修改而不说明验证方式和文档同步情况。
  
  ## 19. 多会话协作规范
  
  多会话协作是指为了隔离角色、阶段、模块或审查视角而开启不同 AI 会话；它不是要求必须使用不同 AI 工具，也不是按工具品牌划分职责。同一个 AI 编程工具的不同会话也必须遵守本规范。
  
  ### 19.1 需要开启新会话的情况
  
  - 从产品规划切换到代码实现；
  - 从架构讨论切换到具体编码；
  - 从实现切换到代码审查；
  - 从 A 模块切换到 B 模块；
  - 当前会话已有大量废弃方案或过期结论；
  - 需求、设计或验收标准发生重大变化；
  - 需要一个独立视角检查当前实现是否符合 Spec。
  
  ### 19.2 推荐会话类型

  | 会话类型      | 主要任务                                   | 产物                                        |
  | ------------- | ------------------------------------------ | ------------------------------------------- |
  | 产品规划会话  | 梳理目标、用户、范围、MVP、验收标准        | `PRD.md`、`docs/Milestones/`                |
  | 架构设计会话  | 方案对比、模块边界、风险和技术验证         | `docs/TechnicalValidation/`、`docs/Design/` |
  | Spec 拆解会话 | 把 PRD 和里程碑拆成可执行规格              | `docs/Specs/`                               |
  | 实现会话      | 按 Spec 或任务卡进行小范围实现             | 源码、测试、配置、文档更新                  |
  | Review 会话   | 检查范围漂移、过度设计、测试缺失、文档遗漏 | Review 记录、修复建议                       |
  | 验收会话      | 按验收标准判断是否可交付                   | `docs/TestReport.md`、`docs/ProcessTodo.md` |
  
  ### 19.3 会话交接规则
  
  不同会话之间不要交接"聊天感觉"，只交接文件化产物：
  
  - PRD；
  - 里程碑；
  - Spec；
  - Todo；
  - 测试报告；
  - 变更摘要；
  - 待确认问题。
  
  新会话开始时，必须明确读取哪些文件、执行什么角色、解决什么问题、不能修改什么范围。
  
  ### 19.4 并行会话规则
  
  - 不要让多个会话同时修改同一批文件。
  - 并行会话必须按模块、文档或任务边界拆分。
  - 合并前必须 Review diff。
  - 如果不同会话结论冲突，以 PRD、Spec、测试结果和用户确认结果为准。
  - 被废弃的会话结论不能继续作为实现依据，必须在后续会话中明确标记为废弃。
  
  ## 20. 禁止事项与反模式
  
  ### 20.1 需求反模式
  
  - "你先做，后面我再看"；
  - "照常规后台系统做"；
  - "用户管理、权限、报表、统计都加上"；
  - "你觉得还需要什么就补上"；
  - "先不用文档"。
  
  ### 20.2 架构反模式
  
  - MVP 阶段引入微服务；
  - 单机小系统上来就做复杂中台；
  - 为不存在的扩展点做大量抽象；
  - 方案没有验证就进入开发；
  - 只看技术先进性，不看部署和维护成本。
  
  ### 20.3 编码反模式
  
  - 一次性改几十个文件；
  - 顺手格式化全仓库；
  - 修 bug 时重构无关模块；
  - 为了通过测试删除测试；
  - 把配置和端口写死；
  - 正式代码直接引用 `webMock/`；
  - 测试没跑却说"应该可以"。
  
  ### 20.4 协作反模式
  
  - 同一个聊天里反复切换产品、设计、研发、测试角色；
  - 多个会话同时改同一文件；
  - 让新会话基于旧会话的长篇聊天继续猜；
  - 不把最终结论沉淀到项目文件；
  - 没有 Review 就合并 AI 代码。
  
  ## 21. 每次任务完成检查清单
  
  完成任何代码、配置、文档或功能变更后，根据任务类型进行相应检查：
  
  ### 21.1 通用必须检查项（所有任务）

  - [ ] **范围控制**：是否只修改了当前任务必须修改的内容
  - [ ] **路径规范**：是否没有写入本机绝对路径
  - [ ] **安全合规**：是否没有硬编码密钥、Token、密码
  - [ ] **测试验证**：是否已运行必要测试（或说明无法运行的原因和替代验证方式）
  - [ ] **结果反馈**：是否已向用户说明验证结果、遗留风险和待确认事项
  
  ### 21.2 代码修改任务检查项

  - [ ] **配置规范**：是否没有使用常用端口，且端口可配置
  - [ ] **配置维护**：是否已提供或维护配置文件（`.env.example` 等）
  - [ ] **文档同步**：是否已更新 `docs/ProcessTodo.md`
  
  **根据影响范围额外检查：**

  - [ ] 如果涉及数据库 → 更新 `docs/DatabaseSchema.md`
  - [ ] 如果涉及部署 → 更新 `docs/Deployment.md` 和 `readme.md`
  - [ ] 如果涉及配置 → 更新 `.env.example` 和 `docs/Config.md`
  - [ ] 如果涉及页面交互 → 更新相关 Spec 或 `docs/Design/`
  
  ### 21.3 文档变更任务检查项

  - [ ] **索引更新**：如果新增、删除、移动或废弃文档，是否已更新 `docs/Index.md`
  - [ ] **状态标记**：文档顶部是否包含状态、最后更新日期、适用范围
  - [ ] **一致性**：文档内容是否与代码、配置保持一致
  
  ### 21.4 快速通道任务检查项（简化流程）

  使用 [2.6 快速通道机制](#26-快速通道机制) 的任务只需检查：

  - [ ] **范围控制**：是否只修改了当前任务必须修改的内容
  - [ ] **安全合规**：是否没有硬编码密钥
  - [ ] **验证说明**：是否简要说明验证结果
  - [ ] **提交信息**：是否在提交信息中说明修改内容
  
  ### 21.5 验收标准

  任务完成必须满足：

  1. 对应任务类型的必须检查项已全部完成
  2. 功能符合 Spec 或任务定义的预期行为
  3. 不做范围未被实现
  4. 无硬编码密钥、端口和本机路径
  5. 用户待确认事项已明确列出
